<section xml:id="ch3sec3">

<title>Image Compression</title>

<p> Suppose that we have a basis
  <m>\bcal=\{\vvec_1,\vvec_2,\ldots,\vvec_m\}</m> for
  <m>\real^m</m>.  What do we mean by the representation
  <m>\coords{\xvec}{\bcal}</m> of
  a vector <m>\xvec</m> in the coordinate system 
  defined by <m>\bcal</m>? </p>
  <p>The components of the vector <m>\coords{\xvec}{\bcal}</m>
    are the weights that express <m>\xvec</m> as a linear
    combination of the basis vectors;  that is,
    <m>\coords{\xvec}{\bcal} =
    \fourvec{c_1}{c_2}{\vdots}{c_m}</m> if
    <m>\xvec=c_1\vvec_1+c_2\vvec_2+\ldots+c_m\vvec_m</m>.</p>

<activity>
      <introduction>
      <p> Since we will be using various bases and the coordinate
      systems they define, let's review how we translate between
      coordinate systems.</p></introduction>
    

  <task><p> If we are given the representation
  <m>\coords{\xvec}{\bcal}</m>, how can we recover the vector
  <m>\xvec</m>?  </p></task>

  <task><p> If we are given the vector <m>\xvec</m>, how can we
  find <m>\coords{\xvec}{\bcal}</m>? </p></task>

  <task><p> Suppose that
  <me>
    \bcal=\left\{\twovec{1}{3},\twovec{1}{1}\right\}
  </me> is a basis for <m>\real^2</m>.  If
  <m>\coords{\xvec}{\bcal} = \twovec{1}{-2}</m>, find the vector
  <m>\xvec</m>.
  <sage>
    <input>
    </input>
  </sage>
  </p></task>

  <task><p> If <m>\xvec=\twovec{2}{-4}</m>, find
  <m>\coords{\xvec}{\bcal}</m>. </p></task>
     
    <!--<solution>
      <p><ol label="a.">
  <li><p>
    The components of the vector <m>\coords{\xvec}{\bcal}</m>
    are the weights that express <m>\xvec</m> as a linear
    combination of the basis vectors;  that is,
    <m>\coords{\xvec}{\bcal} =
    \fourvec{c_1}{c_2}{\vdots}{c_m}</m> if
    <m>\xvec=c_1\vvec_1+c_2\vvec_2+\ldots+c_m\vvec_m</m>.
  </p></li>

  <li><p> If we form the matrix <m>C_{\bcal} =
  \left[\begin{array}{rrrr}
  \vvec_1 \amp \vvec_2 \amp \ldots \amp \vvec_m
  \end{array}\right]</m>, then
  <m>\xvec=C_{\bcal}\coords{\xvec}{\bcal}</m>.
  </p></li>

  <li><p> As before, 
  <m>\coords{\xvec}{\bcal}=C_{\bcal}^{-1}\xvec</m>.
  </p></li>

  <li><p> We find <m>\xvec=C_{\bcal}\coords{\xvec}{\bcal} =
  \twovec{-1}{1}</m>.
  </p></li>

  <li><p> We find
  <m>\coords{\xvec}{\bcal}=C_{\bcal}^{-1}\xvec =
  \twovec{-3}{5}</m>.
  </p></li>
      </ol></p>
    </solution>-->
    </activity>

<!--optional-->

<p>Section 3.3 in Understanding Linear Algebra provides some applications of applications of change of basis, including digital color models and image compression.</p>

<activity><statement>
  <p> Do <url href="https://davidaustinm.github.io/ula/sec-jpeg.html#activity-33" visual="davidaustinm.github.io/ula/sec-jpeg.html#activity-33">Activity 3.3.2 Color models</url> in Understanding Linear Algebra</p>
</statement></activity>





</section>